<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Trạng thái được chọn (selected) -->
    <item android:state_selected="true" android:drawable="@drawable/lucide__support_selected" />

    <!-- Trạng thái mặc định (không được chọn) -->
    <item android:drawable="@drawable/lucide__support" />
</selector>
